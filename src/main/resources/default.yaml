dada:
  tuda:
    framework:
      messaging:
        store-only-cancelable: true
        saga:
          ttl: 10m
          enabled: false
      domains:
        queue:
          naming:
            per-service: true