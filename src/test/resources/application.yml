dada:
  tuda:
    framework:
      messaging:
        store-only-cancelable: false
        saga:
          ttl: 10m
          enabled: true
      domains:
        queue:
          naming:
            per-service: true
spring:
  application:
    name: "test"
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    username: guest
    password: guest
    host: localhost
    port: 5672
    virtual-host: /
    connection-timeout: 2000
logging:
  level:
    org:
      springframework:
        boot:
          context:
            properties: DEBUG
example-3-queue:
  name: example-3-queue